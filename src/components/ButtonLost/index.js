import styled from 'styled-components';

const ButtonLost = styled.button`
    padding: 10px 22px;
    background-color: var(--color-black-dark);
    color: var(--color-gray-light);
    border: 1px solid var(--color-gray-light);
    border-radius: 2px;
    font-size: 16rem;
`;

export default ButtonLost;
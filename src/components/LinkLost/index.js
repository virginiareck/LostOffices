import styled from 'styled-components';

const LinkLost = styled.a`
    font-weight:bold;
    color: var(--color-primary-medium);
    cursor: pointer;
    text-decoration: none;
`;

export default LinkLost;